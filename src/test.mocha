if (5<10) {
    consoleOut("false");
    } else {
      consoleOut("False");
    }

let five = 5;

let ten = 10;

   let add = fn(x, y) {
     x + y;
}

let result = add(five, ten);
consoleOut(result);

consoleOut("test");
