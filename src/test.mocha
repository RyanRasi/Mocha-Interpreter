if (5<10) {
    consoleOut("false")consoleOut("false");
    } elif (5<7) {
    consoleOut("true");
    } else {
      consoleOut("False);
    }

let five = 5;

let ten = 10;

   let add = fn(x, y) {
     x + y;
}

let result = add(five, ten);
consoleOut(result);

consoleOut("test");
